#include <stdio.h>
#include <string.h>


/*
    strlen(string);
    
    strcpy(A, B);   <-- A, B are string && A should be array!!!
    strcpy(A, B, size);     <-- =

    strcmp(A, B);   <-- return (A > B --> pos num, A < B --> neg num, A == B --> return 0)
    strcmp(A, B, size);

    strcat(A, B);   <-- A+B (int python), A should be array!!!
    

   */
